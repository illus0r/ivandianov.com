---
title: "*442 (p4m)"
date: 2026-03-25
---

<!--autotranslate-->

Square lattice with 4-fold rotation at cell centers and corners, plus mirror lines along diagonals and edges.

::: glsl
vec2 uv = (FC.xy * 2. - r) / min(r.x, r.y) * 2.5;
uv=abs(fract(uv*.5)*2.-1.);
if(uv.y>uv.x)uv=uv.yx;
vec3 a=vec3(0,.85,.5),b=vec3(.9,0,.6),c=vec3(.95,.85,0);
o.rgb+=mix(mix(a,b,uv.x),c,uv.y*1.4);
:::

```glsl
vec2 uv = (FC.xy * 2. - r) / min(r.x, r.y) * 2.5;
uv=abs(fract(uv*.5)*2.-1.);
if(uv.y>uv.x)uv=uv.yx;
vec3 a=vec3(0,.85,.5),b=vec3(.9,0,.6),c=vec3(.95,.85,0);
o.rgb+=mix(mix(a,b,uv.x),c,uv.y*1.4);
```

[Live demo on twigl.app](https://twigl.app?ol=true&ss=-OvRAgr0kzDUfl66bfYd)
