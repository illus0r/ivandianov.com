---
title: "442 (p4)"
date: 2026-06-18
---

::: glsl
vec2 uv=(FC.xy*2.-r)/min(r.x,r.y)*2.;
if(mod(floor(uv.y),2.)==0.)uv.y=1.-uv.y;
if(mod(floor(uv.x),2.)==0.)uv.x=1.-uv.x;
if(mod(floor(uv.x)+floor(uv.y),2.)==0.)uv=uv.yx;
uv=fract(uv);
vec3 a=vec3(0,.85,.5),b=vec3(.9,0,.6),c=vec3(.95,.85,0);
o.rgb+=mix(mix(a,b,uv.x),c,uv.y*1.4);
:::

```glsl
vec2 uv=(FC.xy*2.-r)/min(r.x,r.y)*2.;
if(mod(floor(uv.y),2.)==0.)uv.y=1.-uv.y;
if(mod(floor(uv.x),2.)==0.)uv.x=1.-uv.x;
if(mod(floor(uv.x)+floor(uv.y),2.)==0.)uv=uv.yx;
uv=fract(uv);
vec3 a=vec3(0,.85,.5),b=vec3(.9,0,.6),c=vec3(.95,.85,0);
o.rgb+=mix(mix(a,b,uv.x),c,uv.y*1.4);
```

[Живое демо на twigl.app](https://twigl.app?ol=true&ss=-OvUYsRWxM7O9Ce8djeW)
