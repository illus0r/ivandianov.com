---
title: "22× (pgg)"
date: 2026-06-18
---

::: glsl
vec2 uv=(FC.xy*2.-r)/min(r.x,r.y)*2.+t;
if(mod(floor(uv.x)+floor(uv.y),2.)==0.){uv=1.-uv.yx;}
uv=fract(uv);
if(uv.y>uv.x){uv=1.-uv;}
vec3 a=vec3(0,.85,.5),b=vec3(.9,0,.6),c=vec3(.95,.85,0);
o.rgb+=mix(mix(a,b,uv.x),c,uv.y*1.4);
:::

```glsl
vec2 uv=(FC.xy*2.-r)/min(r.x,r.y)*2.;
if(mod(floor(uv.x)+floor(uv.y),2.)==0.){uv=1.-uv.yx;}
uv=fract(uv);
if(uv.y>uv.x){uv=1.-uv;}
vec3 a=vec3(0,.85,.5),b=vec3(.9,0,.6),c=vec3(.95,.85,0);
o.rgb+=mix(mix(a,b,uv.x),c,uv.y*1.4);
```
