# ТЗ: Функция Threads для блога ivandianov.com

**Дата:** 2026-01-30  
**Статус:** Ready for implementation  
**Платформа:** Eleventy 11ty  
**Язык:** Русский (пока), потом английский

---

## 📋 Общее описание

Добавить в блог функцию **Threads** — цепочки коротких связанных постов в стиле Tumblr.

**Примеры использования:**
- G-code эксперименты и настройки 3D-принтера
- GLSL приёмы и техники
- Хроники 3D-печати
- Любые серии связанных заметок

**Основной принцип:** Thread = коллекция постов в одной папке, которые показываются как лента на странице треда и как отдельные посты при клике.

---

## 📁 Структура файлов

### Вариант: Подпапки для каждого поста (выбран)

```
content/ru/threads/
  gcode/
    01-setup/
      index.md
      photo1.jpg
      photo2.jpg
    02-fan/
      index.md
      result.jpg
    03-calibration/
      index.md
  shaders/
    01-noise/
      index.md
      demo.gif
```

**Почему этот вариант:**
- ✅ Изоляция: каждый пост = отдельная папка
- ✅ Картинки рядом с контентом
- ✅ Чисто, легко добавлять новые посты

### Картинки в assets (после копирования)

```
assets/threads/gcode/
  01-setup/
    photo1.jpg
    photo2.jpg
  02-fan/
    result.jpg
```

**Копирование:** Eleventy должен копировать `.jpg`, `.png`, `.gif` из `content/ru/threads/**/*.{jpg,png,gif}` в `assets/threads/`.

---

## 🔗 URL структура

### Thread index (лента постов)
```
/ru/threads/gcode/          → content/ru/threads/gcode/ (все посты)
/en/threads/shaders/        → content/en/threads/shaders/
```

### Отдельный пост
```
/ru/threads/gcode/setup/    → content/ru/threads/gcode/01-setup/index.md
/ru/threads/gcode/fan/      → content/ru/threads/gcode/02-fan/index.md
```

**Slug:** Берётся из имени папки БЕЗ номера.
- `01-setup` → slug: `setup`
- `02-fan` → slug: `fan`
- `03-long-name` → slug: `long-name`

**Номер в имени папки** — только для сортировки (не показывается в UI).

---

## 📝 Front matter

**Все поля опциональны.**

### Минимальный валидный пост
```yaml
---
# можно вообще пустой front matter или даже без него
---

Какой-то текст или картинки.
```

### Полный пример
```yaml
---
title: "Настройка вентилятора"
date: 2026-01-30
images:
  - /assets/threads/gcode/02-fan/before.jpg
  - /assets/threads/gcode/02-fan/after.jpg
---

Заменил вентилятор на Noctua. Стало тише!
```

### Поля

| Поле | Тип | Обязательно | Описание |
|------|-----|-------------|----------|
| `title` | string | нет | Заголовок поста. Если нет — не показывать `<h1>` |
| `date` | YYYY-MM-DD | нет | Дата публикации. Показывать если есть |
| `images` | array | нет | Массив полных путей к картинкам |

**Текст поста** — markdown после front matter. Может отсутствовать (только картинки).

---

## 🎨 Отображение

### Thread index (`/threads/gcode/`)

**Формат:** Вертикальная лента постов (как Tumblr).

**Каждый пост показывает:**
1. **Заголовок** (если есть `title`) — ссылка на `/threads/gcode/setup/`
2. **Дата** (если есть `date`) — мелким шрифтом, как в текущем блоге
3. **Все картинки** подряд вертикально
4. **Весь текст** (полностью, не excerpt)

**Картинки:**
- Выравнивание: `align: left`
- Масштабирование: без `max-height`, но умное (портретные 9:16 не растягивать во всю ширину)
- Размер: не больше оригинального
- Все картинки подряд вертикально (одна под другой)

**Стили:** Переиспользовать текущие стили блога. Ничего нового не придумывать.

**Разделитель между постами:** Как в текущем блоге (линия или отступ).

---

### Отдельный пост (`/threads/gcode/setup/`)

**Контент:** То же самое, что в ленте (заголовок, дата, картинки, текст).

**Навигация:**

1. **Стрелка назад** — рядом с заголовком (или на том же уровне, если заголовка нет):
   ```
   ← /threads/gcode/
   ```
   Ссылка на thread index.

2. **Навигация между постами** — внизу страницы:
   ```
   ← Предыдущий | Следующий →
   ```
   - Если это первый пост — скрыть "← Предыдущий"
   - Если это последний пост — скрыть "Следующий →"

**Стили:** Как в текущих постах блога.

---

### Ссылки на треды в `/blog/`

**Расположение:** Над списком постов (на самом верху).

**Формат:** Простые ссылки (без карточек):
```
Треды: [G-code] [Shaders] [3D-печать]

─────────────────────────────

[Обычные посты блога...]
```

**Стили:** Как обычные ссылки, можно чуть выделить (жирным или цветом).

**Порядок:** Алфавитный или по дате создания (на усмотрение разработчика).

---

## 🧭 Техническая реализация

### Eleventy Collections

Создать коллекцию для каждого треда:

```js
// .eleventy.js
eleventyConfig.addCollection("threadGcode", function(collectionApi) {
  return collectionApi.getFilteredByGlob("content/ru/threads/gcode/*/index.md")
    .sort((a, b) => {
      // Сортировка по номеру в имени папки
      const aNum = parseInt(a.filePathStem.match(/(\d+)-/)?.[1] || 0);
      const bNum = parseInt(b.filePathStem.match(/(\d+)-/)?.[1] || 0);
      return aNum - bNum;
    });
});
```

### Slug генерация

Убрать номер из slug:
```js
// Пример permalink в front matter или data file
permalink: "/ru/threads/gcode/{{ page.fileSlug | removeNumber }}/"

// Фильтр removeNumber:
eleventyConfig.addFilter("removeNumber", function(slug) {
  return slug.replace(/^\d+-/, '');
});
```

### Копирование картинок

```js
eleventyConfig.addPassthroughCopy({
  "content/**/threads/**/*.{jpg,jpeg,png,gif,webp}": "assets/threads/"
});
```

### Получение списка тредов

```js
eleventyConfig.addCollection("allThreads", function(collectionApi) {
  const threads = new Set();
  collectionApi.getFilteredByGlob("content/ru/threads/*/*/index.md")
    .forEach(post => {
      const match = post.filePathStem.match(/threads\/([^\/]+)\//);
      if (match) threads.add(match[1]);
    });
  return Array.from(threads).sort();
});
```

---

## 🌐 Мультиязычность

**Сейчас:** Только русский (`/ru/threads/`).

**Потом:** Английский (`/en/threads/`).

Переводы делаются вручную (дублирование структуры в `/content/en/threads/`).

---

## 📐 Картинки: умное масштабирование

**Проблема:** Портретные фото (9:16) не должны растягиваться во всю ширину блока.

**Решение:**
```css
.thread-image {
  max-width: 100%;
  height: auto;
  display: block;
  margin: 1rem 0;
}

/* Портретные — выравниваем влево, ограничиваем ширину */
.thread-image[data-orientation="portrait"] {
  max-width: 400px; /* или другое разумное значение */
}
```

**Определение ориентации:** Можно через JS или задавать вручную в front matter (если нужно).

---

## ✅ Чек-лист реализации

### Файловая структура
- [ ] Создать `/content/ru/threads/gcode/01-test/index.md` (пример)
- [ ] Добавить 2-3 картинки в тестовый пост
- [ ] Настроить копирование картинок в assets

### Eleventy конфигурация
- [ ] Коллекции для каждого треда
- [ ] Коллекция всех тредов (для списка на /blog/)
- [ ] Фильтр `removeNumber` для slug
- [ ] Permalink для постов тредов

### Шаблоны
- [ ] Шаблон для thread index (`threads/[name].njk` или подобное)
- [ ] Шаблон для отдельного поста треда
- [ ] Добавить ссылки на треды в `/blog/` (шаблон страницы блога)

### Навигация
- [ ] Стрелка ← назад к thread index (в шапке поста)
- [ ] Навигация ← → между постами (внизу страницы)
- [ ] Скрывать стрелки на краях (первый/последний пост)

### Стили
- [ ] Переиспользовать текущие стили блога
- [ ] Умное масштабирование картинок (портрет vs ландшафт)
- [ ] Выравнивание картинок влево

### Тестирование
- [ ] Проверить thread index (лента)
- [ ] Проверить отдельный пост
- [ ] Проверить навигацию между постами
- [ ] Проверить ссылки на /blog/
- [ ] Проверить пост без заголовка
- [ ] Проверить пост без даты
- [ ] Проверить пост с только картинками (без текста)

---

## 🎯 Примеры контента для тестирования

### Пост 1: Полный
```yaml
---
title: "Настройка принтера"
date: 2026-01-30
images:
  - /assets/threads/gcode/01-setup/photo1.jpg
  - /assets/threads/gcode/01-setup/photo2.jpg
---

Первый запуск принтера. Калибровка стола.
```

### Пост 2: Без заголовка
```yaml
---
date: 2026-01-31
images:
  - /assets/threads/gcode/02-fan/result.jpg
---

Поменял вентилятор.
```

### Пост 3: Только картинка
```yaml
---
images:
  - /assets/threads/gcode/03-print/final.jpg
---
```

### Пост 4: Только текст
```yaml
---
title: "Заметка"
date: 2026-02-01
---

Нужно заказать новые сопла.
```

---

## 📚 Референсы

**Дизайн:** Как текущий блог на ivandianov.com  
**Формат:** Tumblr (вертикальная лента)  
**Навигация:** Sequences у Элизера Юдковского (стрелки вперёд/назад)

---

## 🚀 Готовность к разработке

Все решения приняты. Можно начинать реализацию.

**Следующий шаг:** В новой сессии загрузить этот файл и начать кодить.
